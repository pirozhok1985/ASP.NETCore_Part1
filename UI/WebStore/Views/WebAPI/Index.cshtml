@model IEnumerable<string>

@{
    ViewBag.Title = "WebAPI test";
    var i = 1;
}
<div class="container">
    <table class="table">
        <thead>
        <tr>
            <th>№</th>
            <th>Value</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var value in Model)
        {
            <tr>
                <td>@(i++)</td>
                <td>@value</td>
            </tr>
        }
        </tbody>
    </table>
</div>